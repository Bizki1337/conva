import type {
  IAnimation,
  IFrame,
  DirectionType,
  HeroActionsType,
} from './animationInterface';
import type { HexTagType, IHex } from './mapInterfaces';

export type {
  HexTagType,
  IHex,
  IAnimation,
  IFrame,
  DirectionType,
  HeroActionsType,
};
